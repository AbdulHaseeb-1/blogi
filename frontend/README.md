# Blogi Frontend

A standalone, static front-end experience for the Blogi editorial site, built with HTML + CSS in a stone theme.

## Run locally
Open `index.html` directly in your browser or serve the folder via a simple web server:

```
cd frontend
python3 -m http.server 5050
```

Then visit `http://localhost:5050`.
